import { NavLink } from "react-router-dom";

const Header = () => {
  return (
    <header>
      <NavLink to="/">
        <img src="" alt="instagram-clone" />
      </NavLink>
    </header>
  );
};

export default Header;
